# 「关于见你这件事」约会 App 设计与开发计划

> 英文名：**The Thought of Seeing You**

> 项目定位：制作一个发给 crush 的手机端优先约会邀请网页。保留参考视频中轻松、直接、一步步完成约会选择的核心体验，同时将偏“表单工具”的背景和细节升级为更浪漫、有仪式感的视觉体验。

## 1. 作品构思

### 1.1 核心概念

作品正式命名为 **「关于见你这件事」**，英文名为 **The Thought of Seeing You**。

它不是一份普通的约会问卷，而是一封可以互动的电子情书：对方从打开邀请、选择时间、约会方式和想吃的东西，到最终收下一张专属约会凭证，整个过程像拆开一封层层展开的信。

核心体验关键词：

- **心动**：柔和、温暖、甜而不腻。
- **轻松**：文案自然俏皮，不给对方太大压力。
- **参与感**：由 crush 自己选择约会时间和偏好。
- **仪式感**：最终生成完整的约会邀请卡，而不是普通提交成功页。
- **端正耐看**：手机端沉浸，电脑端居中舒展，文字和元素不拉伸、不歪斜。

### 1.2 情绪曲线

整个作品按以下情绪递进展开：

1. **好奇**：看到一封“只给你”的心动邀请。
2. **被认真对待**：可以选择自己舒服的时间。
3. **共同期待**：选择想怎样见面、想吃什么。
4. **小惊喜**：每次选择都有细腻的动画与情感化回应。
5. **确定与收藏**：生成一张可以保存的约会凭证，并显示见面倒计时。

### 1.3 视觉方向

参考视频使用了清新的新粗野主义卡片风格，但浅灰绿背景和原生表单略显像工具。本项目保留其清晰、可点击的大卡片结构，改造成 **“柔光手账 × 浪漫信笺”** 风格。

#### 色彩

- 主背景：奶油白、雾粉、蜜桃色与淡紫色组成的柔和弥散渐变。
- 主强调色：莓果粉或玫瑰粉，用于主要行动按钮。
- 辅助色：香槟金、鼠尾草绿，只做小面积点缀和选中反馈。
- 正文颜色：深可可灰，避免纯黑带来的生硬感。
- 卡片：半透明奶油白，搭配柔和阴影、细边框和轻微纸张纹理。

#### 背景

- 使用缓慢流动的弥散光影，而不是单一纯色。
- 加入少量、低密度的漂浮爱心、花瓣或星点，保持浪漫但不干扰阅读。
- 背景动画遵循低频、低振幅原则，避免廉价感和眩晕感。
- 在系统开启“减少动态效果”时自动降低或关闭背景动画。

#### 字体与排版

- 中文正文优先使用清晰、端正的现代无衬线字体。
- 标题可搭配少量有手写感的字体，但只用于短句，不影响可读性。
- 正文字号不小于 14px，手机端主要正文建议 16px。
- 所有标题、选项和按钮保持稳定的字重与行高，电脑端不因容器变宽而过度放大。

#### 动效

- 页面切换：淡入、轻微上移或信纸翻页效果。
- 选项反馈：边框柔和亮起、爱心勾选图标出现、卡片轻微回弹。
- 主按钮：点击后产生轻微光晕或心跳感，不做过度缩放。
- 最终确认：短暂的爱心纸屑或花瓣庆祝动画。
- 所有动画以流畅、克制为原则，单次反馈尽量控制在 150–500ms。

## 2. 参考视频拆解与继承范围

### 2.1 参考视频的核心流程

参考视频中的主要功能链路为：

1. 选择见面日期和时间。
2. 补充可接受的时间范围，可多选。
3. 选择约会形式，例如散步、吃饭、看电影等。
4. 选择想吃的类型，例如日料、烧肉、粤菜、火锅、西餐等。
5. 自动汇总前面选择的信息。
6. 生成“约会小约定”结果卡。
7. 提供“确认收下爱意”和“保存图片”等操作。

### 2.2 必须保留的体验

- 分步骤引导，不将所有问题堆在同一页。
- 日期与时间选择，其中时间偏好支持多选。
- 时间与餐饮选项支持多选，并提供明确的选中反馈。
- 多选能力通过卡片勾选状态自然表达，前端不出现“可以多选”“支持多选”等说明性文案。
- 底部主按钮根据当前状态改变可用性与文案。
- 后续页面自动继承前面已选择的信息。
- 最终汇总时间、约会方式和餐饮偏好。
- 生成可保存的约会凭证。

### 2.3 不直接照搬的部分

- 不使用冷淡的浅灰绿纯色背景。
- 不直接采用视觉生硬的原生日期/时间输入框作为主要界面。
- 不照搬“地址弄好了”等偏工具化文案。
- 前端不显式写出“可以多选”，以统一的复选状态和选中反馈让用户自然理解操作方式。
- 不使用过粗的纯黑边框和过硬阴影，以免破坏柔和氛围。
- 不默认加入“拒绝按钮逃跑”等有压迫感的设计；如后续需要，可作为可关闭的俏皮彩蛋，而不是阻止对方正常选择。

## 3. 页面与功能规划

### 3.1 开场页：只给你的邀请

**目标：** 建立第一眼的浪漫感和好奇心。

建议内容：

- 小标题：“一封只给你的邀请”
- 主标题：“想认真地，约你出去一次”
- 简短副文案：“不用现在就回答所有问题，跟着感觉慢慢选就好。”
- 主按钮：“拆开看看”
- 可选装饰：信封、花瓣、两颗逐渐靠近的小星星。

### 3.2 时间选择页

**目标：** 选择具体日期和时间，同时弱化表单感。

功能：

- 自定义日历卡片，突出今天、周末和选中日期。
- 具体日期作为首选约会日期；时间偏好采用卡片式多选，而不是只能选择一个时间点。
- 时间卡片可同时勾选多个，选中时显示统一的爱心勾选与柔和高亮；页面不额外标注“可以多选”。
- 选择后立即显示自然语言摘要，例如：“午后、傍晚或晚饭后，我都期待见到你。”
- 未完成选择时，主按钮保持柔和禁用状态。

主按钮建议文案：“把这些时间悄悄告诉你”

### 3.3 备选时间页

**目标：** 在首选时间不方便时保留弹性。

功能：

- 采用两列卡片网格，参考图中的“图标 + 自然语言时间 + 勾选状态”结构。
- 时间项扩充为：今天晚上、明天、这个周末、下个周末、工作日晚上、午后、傍晚、晚饭后、夜一点也行、早午餐时间、临时约也可以、你来定。
- 所有时间项均可组合选择；不在标题、卡片或按钮文案中显式提示“多选”。
- 保留一个“你来定”兜底选项，把决定权自然地交给对方。
- 底部显示首选日期与当前时间偏好的自然语言摘要。
- 至少选择一项后才可继续。

情感化文案：“如果那天不巧，这些时候我也愿意为你留出来。”

### 3.4 约会方式页

**目标：** 让 crush 决定这次见面的氛围。

候选卡片：

- 轻松散步
- 吃点喜欢的
- 看一场电影
- 找家店慢慢聊天
- 一起逛逛
- 交给你安排

交互：

- 支持单选；如果需要兼容“都想要”，可增加“这几个都不错”。
- 卡片选中时出现柔和高亮、勾选图标和一条对应的小文案。

### 3.5 餐饮偏好页

**目标：** 延续参考视频的选择矩阵，但提升图标和文案质感。

候选内容：

- 日料、烧肉、粤菜、火锅、西餐、甜品、咖啡、夜宵、早午餐、小酒馆、街边小吃、惊喜安排。
- 餐饮偏好首版即支持多选，可同时保留多种想吃的方向。
- 前端不显示“可以多选”等说明，通过卡片右上角勾选状态、选中高亮和已选摘要自然表达。
- 使用统一风格的小插画或图标，不混用风格差异较大的 Emoji。
- 至少选择一项；选择“惊喜安排”时仍允许同时勾选其他偏好，供后续安排参考。

### 3.6 Dating 卡片生成与保存页

**目标：** 将前面的选择生成一张真正可收藏、可分享的 Dating 卡片，形成完整闭环。

页面内容：

- 标题：“真开心，你收下了这份邀请。”
- 自动汇总已选日期、多个时间偏好、约会方式和多个餐饮偏好。
- 一句收尾文案：“我会带着期待，准时来见你。”
- 主按钮：“生成 Dating 卡片”
- 生成后显示卡片预览，并提供“保存到相册”和“分享”操作。
- 确认后显示动态倒计时：“距离见到你还有 X 天 X 小时。”

Dating 卡片设计：

- 首版输出适合手机相册与社交媒体的竖版高清图片，建议比例为 9:16，同时确保微信聊天预览时主体信息清楚。
- 卡片包含作品标题、双方称呼、日期、时间偏好、约会方式、餐饮偏好、浪漫收尾文案和署名。
- 使用本地素材库中的透明 PNG 或完整浪漫场景作为主视觉，保证离线生成时素材不丢失。
- 多个时间和餐饮选项采用短标签自动换行；超出安全区域时自动缩小间距或调整排版，不截断文字。
- 卡片预览与最终导出图片应保持一致，避免字体错位、图片模糊或内容裁切。

生成与保存：

- 在浏览器前端将卡片渲染为高清图片，不依赖后端截图服务。
- 导出时按设备像素比提升清晰度，并等待字体与图片素材加载完成后再生成。
- 手机端点击“保存到相册”时生成 PNG 文件；受浏览器权限限制无法直接写入相册时，触发图片下载或打开图片，并提示用户长按保存。
- 文件名使用可识别格式，例如 `Dating-Card-2026-09-20.png`。
- 生成或保存失败时保留当前选择，允许重新生成，不要求用户重新填写。

分享策略：

- 优先调用浏览器原生分享能力，将生成的 Dating 卡片图片作为文件分享到微信等已安装应用。
- 仅在浏览器支持文件分享且当前环境满足安全要求时显示或启用“分享”按钮。
- 微信内置浏览器、桌面浏览器或不支持文件分享的环境，自动降级为“保存图片”，并提示用户保存后从微信发送。
- 分享属于增强能力；首版必须优先保证“生成卡片 + 保存图片”稳定可用。

其他可选增强：

- 一键添加到系统日历。
- 将选择结果以链接参数或本地数据方式保留，刷新后不丢失。

## 4. 文案基调

### 4.1 基本原则

- 真诚，不油腻。
- 俏皮，但不通过强迫或戏弄制造压力。
- 少用连续感叹号和过多 Emoji。
- 每一页只保留一句核心情绪文案，避免信息过载。
- 给 crush 保留舒适的选择空间。

### 4.2 示例语气

- “跟着感觉选就好，我都认真看。”
- “这一天如果刚好有空，我想把它留给你。”
- “散步也好，吃饭也好，重点是和你一起。”
- “已经开始期待见到你了。”

## 5. 响应式适配要求

### 5.1 手机端优先

- 主要适配宽度：320px–480px。
- 页面高度支持短屏和长屏，不依赖固定 9:16 高度。
- 内容区宽度占屏幕大部分，但左右保留安全边距。
- 主要按钮触控高度不低于 44px。
- 底部操作区考虑浏览器工具栏和安全区域，不遮挡内容。
- 选项较多时允许页面自然滚动，主按钮可采用不遮挡内容的吸底方案。

### 5.2 电脑端要求

- 页面整体置于居中的竖向体验容器中，但不做简单的“手机截图式放大”。
- 背景在宽屏上自然延展，主体卡片保持舒适宽度，避免文字行过长。
- 字号采用有限范围的响应式缩放，确保标题和正文端正、稳定。
- 卡片、按钮、图标保持正常比例，不横向拉伸。
- 鼠标悬停、键盘焦点和点击状态都应有明确反馈。
- 电脑端视觉可加入少量左右装饰，但不得抢占主体注意力。

### 5.3 建议断点策略

- 小屏手机：单列、紧凑间距。
- 常规手机：单列、完整装饰与动效。
- 平板及电脑：居中主卡片，背景扩展，部分选项可根据内容使用两列或三列。
- 超宽屏：限制主体最大宽度，避免内容被无限拉开。

## 6. 状态与交互规则

- 每一步都保存当前选择，返回上一页时保留已选内容。
- 未满足必选条件时，不允许误进入下一步，并给出温和提示。
- 页面切换后自动将焦点移动到新页面标题，兼顾键盘与无障碍体验。
- 所有图标按钮提供可读标签。
- 动效失败或设备性能较弱时，不影响核心流程。
- 保存图片失败时展示可理解的提示，并允许重试。
- 刷新页面后可恢复当前进度；首版可优先使用浏览器本地存储。
- 首版默认不接后端，不收集对方隐私信息；如后续需要把结果自动发送给发起者，再单独设计明确授权和数据方案。

## 7. 内容配置项

为了后续方便个性化，以下内容应设计为可集中修改，而不是散落在页面代码中：

- 发起人称呼与 crush 称呼。
- 开场标题、副文案和收尾文案。
- 可选日期范围与可选时间段。
- 约会方式列表。
- 餐饮偏好列表。
- 最终卡片署名。
- 背景音乐开关与音源。
- 是否显示“我再想想”等可选分支。
- 是否启用倒计时和添加日历功能。

### 7.1 素材资产管理

- 用户提供的 10 张图标合集与场景图保存在 `assets/source/icon-sheets/`。
- 用户另附的 4 张透明 PNG 图标合集保存在 `assets/source/transparent-icon-sheets/`，已验证包含有效透明通道。
- 素材用途、内容和裁切建议统一记录在 `assets/README.md`。
- 原始素材只读保留；开发时将裁切结果放入 `assets/generated/icons/`，不覆盖源文件。
- 一张图包含多个小图标时，按网格裁切并使用语义化文件名，保留必要的光晕、阴影和半透明边缘。
- 页面优先选用电影票、餐桌、摩天轮、咖啡、甜品、礼物、烟花、流星和爱心等与当前语义匹配的图标。
- Dating 卡片可从完整浪漫场景中选择背景，再叠加透明 3D 图标作为前景装饰。

## 8. 开发阶段规划

### 阶段 A：结构原型

- 搭建完整分步流程。
- 完成时间、备选时间、约会方式、餐饮偏好选择逻辑。
- 完成状态保存、返回修改和最终汇总。
- 先验证手机端流程是否顺畅。

### 阶段 B：视觉成型

- 应用柔光手账风视觉系统。
- 完成自定义日历、选项卡片和约会凭证样式。
- 完成手机、平板、电脑端响应式布局。
- 重点检查电脑端字体、按钮和卡片是否端正。

### 阶段 C：动效与浪漫氛围

- 加入弥散光背景与低密度漂浮元素。
- 加入页面转场、选中反馈、确认庆祝动画。
- 增加减少动态效果模式。
- 可选加入由用户主动开启的背景音乐；不强制自动播放。

### 阶段 D：Dating 卡片生成、保存与分享

- 根据所有已选信息生成竖版高清 Dating 卡片。
- 完成卡片预览与 PNG 保存功能，优先保证手机相册保存链路。
- 接入浏览器原生文件分享能力，在支持的移动端调用系统分享面板。
- 为微信内置浏览器和不支持文件分享的环境提供“保存后发送”的降级提示。
- 验证多个时间与多个餐饮标签在导出图片中的自动排版。
- 完成见面倒计时。
- 视需要增加“一键添加到日历”。

### 阶段 E：验证与上线

- 覆盖常见手机宽度、短屏、平板、普通电脑和超宽屏。
- 验证触控、鼠标、键盘操作。
- 检查文字换行、图片清晰度、动效流畅度和保存图片结果。
- 完成正式构建并部署为可直接分享的网页链接。

## 9. 验收标准

- 能从开场顺利完成全部选择并进入约会凭证页。
- 每一步选择都会被正确保留和汇总。
- 手机端主要操作无需横向滚动，按钮不被浏览器底栏遮挡。
- 电脑端字体、图标、按钮和卡片均不变形、不失衡。
- 背景明显比参考视频更浪漫，但不影响文字阅读和按钮操作。
- 动画流畅且不过度；关闭动画后仍可正常使用。
- Dating 卡片预览与最终 PNG 内容一致，文字、字体、图标和背景均不缺失、不模糊、不裁切。
- 生成后的 Dating 卡片可在手机端正常下载或通过长按保存到相册。
- 支持原生文件分享的浏览器可唤起系统分享面板；不支持时不会报错，并明确引导先保存图片。
- 多个时间和多个餐饮标签在卡片中自动换行且保持清晰层级。
- 页面加载失败、卡片生成失败或保存失败时有清楚、温和的反馈。
- 不默认收集或上传 crush 的个人信息。

## 10. 暂不纳入首版

- 用户注册与登录。
- 复杂后端数据库。
- 在线支付、地图路线和订餐功能。
- 未经明确同意自动获取位置、通讯录等信息。
- 过度复杂的 3D 场景。
- 强制阻止拒绝、无法退出或无限逃跑的按钮。

## 11. 更新迭代记录

> 维护规则：每次对需求、设计、功能或技术实现做出调整，都在本节新增一条记录。记录需包含日期、版本、变更内容、变更原因和后续待办；旧记录保留，不覆盖。

### v0.1 — 2026-09-15｜完成初始规划

**本次变更**

- 分析参考视频中的时间选择、备选时间、约会方式、餐饮偏好和约会凭证流程。
- 确定保留“分步选择 + 自动汇总 + 保存凭证”的核心功能。
- 将视觉方向从浅灰绿工具感升级为“柔光手账 × 浪漫信笺”。
- 明确手机端优先，同时要求电脑端字体与元素保持端正、不拉伸。
- 增加倒计时、保存图片和可选添加日历等增强方向。
- 明确首版不默认使用带压迫感的拒绝按钮逃跑机制。

**变更原因**

- 用户希望功能与参考作品相似，但背景更浪漫。
- 作品将直接发给 crush，需要兼顾情绪氛围、轻松感和使用舒适度。

**下一步**

- 根据本计划建立网页项目骨架。
- 先完成可点击的结构原型，再统一处理视觉与动效。
- 开发前确定称呼、开场文案、可选时间范围及约会选项是否需要个性化。

---

### v0.2 — 2026-09-15｜调整时间与餐饮选择规则

**本次变更**

- 时间偏好调整为多选，并参考补充图片改为两列卡片网格。
- 时间项扩充为 12 个，覆盖日期范围、具体时段、临时邀约与“你来定”等不同表达。
- 餐饮偏好调整为首版即支持多选，并扩充至 12 个选项。
- 前端不显示“可以多选”“支持多选”等说明性文案，改由勾选状态、选中高亮和结果摘要自然传达。

**变更原因**

- 为 crush 提供更多可接受时间与餐饮方向，提高约会匹配成功率。
- 避免界面出现偏功能说明式文案，保持自然、浪漫的对话感。

**验证结果**

- 规划中的时间和餐饮交互规则已统一为多选。
- 已清除首版餐饮默认单选的旧设定。
- 时间项数量和布局已按参考图片补充。

**下一步**

- 开发时验证两列网格在 320px 小屏手机上的文字换行与点击区域。
- 确认最终凭证页对多个时间和多个餐饮选项的摘要展示方式。

---

### v0.3 — 2026-09-15｜归档视觉素材并确认 Dating 卡片能力

**本次变更**

- 归档用户提供的 10 张图标合集、场景图和 4 张透明 PNG 图标合集。
- 建立素材目录与用途清单，明确原图保留、按需剪裁和语义化命名规则。
- 将最终结果页升级为 Dating 卡片生成页，支持预览与高清 PNG 导出。
- 将“生成卡片 + 保存图片”设为首版必做能力。
- 增加浏览器原生文件分享方案，可用时分享到微信等应用，不可用时降级为保存图片后发送。
- 补充多个时间、多个餐饮偏好在卡片中的自动换行和自适应排版要求。

**变更原因**

- 用户提供了可直接用于作品的小图标和透明 PNG 素材，希望开发过程中随时取用。
- 最终作品需要产出一张可收藏、可保存、尽可能可直接分享的 Dating 卡片。

**验证结果**

- 10 张素材已保存至 `assets/source/icon-sheets/`。
- 4 张透明素材已保存至 `assets/source/transparent-icon-sheets/`，且已确认包含有效 Alpha 通道。
- 素材清单已写入 `assets/README.md`。
- Dating 卡片的生成、保存、分享降级与验收条件已写入开发计划。

**下一步**

- 开发时按页面语义从素材合集中裁切所需图标，并保存到 `assets/generated/icons/`。
- 优先完成卡片生成与保存，再验证不同手机浏览器的原生分享兼容性。

---

### 后续记录模板

#### vX.X — YYYY-MM-DD｜迭代标题

**本次变更**

- 

**变更原因**

- 

**验证结果**

- 

**下一步**

- 


---

### v0.4 — 2026-09-15｜完成开发、验证与上线

**本次变更**

- 使用 `aime_create_web_app` 初始化 `the-thought-of-seeing-you`，按 EdenX、React 19、TypeScript 与 Tailwind 默认技术栈完成前端实现。
- 完成浪漫开场、日期与具体时间、12 项备选时间、约会方式、12 项餐饮偏好、Dating 卡片生成共 6 个页面状态；时间与餐饮通过勾选、高亮和摘要自然支持组合选择。
- 实现返回保留、浏览器本地持久化、刷新恢复、逐步必填校验、页面标题焦点管理、键盘焦点样式、语义标签及 `prefers-reduced-motion` 降级。
- 从用户提供的透明图标合集中按语义裁切信封、日历、餐桌、咖啡、时钟、烟花等独立 PNG，保留透明边缘，并在页面真实使用；原素材未修改。
- 完成 9:16 竖版 Dating 卡片预览与 3 倍像素 PNG 导出，汇总日期、多个首选时间、备选时间、约会方式和多个餐饮偏好；支持 Web Share API 文件分享，不支持或失败时自动降级下载，并提供长按保存提示。
- 完成柔光渐变、纸张纹理、低密度漂浮装饰、移动端安全区和桌面居中限宽布局；未提供暗色模式，也不收集或上传用户信息。

**验证结果**

- `pnpm run build` 通过，EdenX 生产构建成功，类型检查通过。
- 业务页面 `src/routes/page.tsx` 与 `src/routes/index.css` 的 Biome 检查通过；全仓库 `pnpm run lint` 仍会报告脚手架预置 `src/components/ui` 与受保护配置文件的历史格式诊断，本次未改动这些模板文件。
- 使用规范指定的 `html_vision` 对 390×844 手机视角和 1440×900 桌面视角进行检查：主卡片居中，素材清晰，文字和按钮无拉伸、遮挡或横向溢出；修正辅助文字对比度后完成回归。
- 部署后再次通过 `html_vision` 验证页面可访问、静态资源完整、控制台无本应用运行时错误。

**部署结果**

- 已上线：<https://7d4de09f9401.aime-site.bytedance.net>
- 项目目录：`the-thought-of-seeing-you/`

**已知兼容性限制**

- Web Share 文件分享依赖 HTTPS、浏览器实现及系统能力；不支持文件分享的微信内置浏览器或桌面浏览器会自动改为下载 PNG。
- 浏览器出于安全限制通常不能直接写入系统相册，移动端需通过下载结果或长按生成后的预览图保存。
- 倒计时按所选日期的本地时区计算到天；跨时区打开时可能出现一天以内的显示差异。

**下一步**

- 如需进一步个性化，可集中调整双方称呼、署名、开场文案和候选日期范围。
- 可在后续版本补充添加到系统日历与更多移动浏览器真机回归测试。


---

### v0.5 — 2026-09-15｜更新首页主标题

**本次变更**

- 将首页主标题由“想认真地，约你出去一次”调整为“关于见你这件事”。
- 保留英文副标题 “THE THOUGHT OF SEEING YOU”。

**验证结果**

- `pnpm run build` 通过，类型检查与生产构建成功。
- 已重新部署至原访问地址。
- 在线视觉检查确认新标题、背景和信封素材显示正常，旧文案已移除。


---

### v0.6 — 2026-09-15｜收窄页面并优化 Dating 卡片可读性

**本次变更**

- 删除首页“只保存在你的浏览器里 · 不收集任何信息”说明。
- 页面主体统一收窄为接近 iPhone 的 402px 最大宽度，电脑端仅居中展示，不再随桌面宽度扩张。
- 页面高度限制在视口内，移除浏览器横向和纵向滚动条。
- Dating 卡片背景调整为更浅的奶油粉柔光效果，信息卡片改为浅色半透明材质。
- 放大 Dating 卡片标题、标签、日期、时间、约会方式、餐饮偏好和落款文字。
- 缩小最终页预览尺寸并压缩垂直间距，使卡片预览及生成、保存、分享操作可在单屏内完成。
- 生成后的高清图片直接覆盖原卡片预览，不再在页面下方重复插入第二张图片，避免页面变长。

**验证结果**

- 业务页面 Biome 检查通过。
- `pnpm run build` 与 TypeScript 类型检查通过。
- 电脑端视觉检查确认页面为 iPhone 窄版居中布局，无横向或纵向滚动条，首页说明文案已移除。


---

### v0.7 — 2026-09-15｜修复卡片排版并准备 GitHub 源码包

**本次变更**

- 修复 Dating 卡片底部备选时间与落款重叠的问题。
- 固定三行信息网格的可用区域，标签不再异常断行，正文保持清晰层级。
- 降低背景场景饱和度并加强奶油粉蒙层，标题和信息卡片统一为轻盈的半透明信笺材质。
- 新增标题信息区背景，调整标题、标签、正文与落款的间距和字号，使 9:16 卡片更协调。
- 修改本地状态恢复逻辑：保留用户之前的选择，但每次通过网页链接打开时固定从首页开始。
- 重新完成生产构建并部署至原访问地址。
- 生成适合上传 GitHub 的完整源码压缩包，包含项目源代码、依赖锁文件、处理后素材、原始素材与项目规划；排除 `.git`、`node_modules`、`dist` 和本地环境文件。

**验证结果**

- 业务代码 Biome 检查通过。
- `pnpm run build` 与 TypeScript 类型检查通过。
- 部署后直接打开链接可正常显示首页，素材和布局加载正常。
- ZIP 完整性测试通过。


---

### v0.8 — 2026-09-15｜彻底修复 Dating 卡片标题重叠

**本次变更**

- 根据最新截图确认重叠源于主标题在窄卡片内占用两行并向下侵入 DATE / TIME 信息区。
- 将卡片主标题缩放为适合 9:16 导出宽度的单行尺寸，并设置强制单行、居中和溢出保护。
- 重新划定标题区与详情信息区的独立范围，在两者之间保留明确安全间距。
- 保持其他已经正常的背景、信息卡片、落款和页面尺寸不变。
- 重新完成构建、部署并更新 GitHub 源码压缩包。

**验证结果**

- CSS 代码检查通过。
- `pnpm run build` 与 TypeScript 类型检查通过。
- 新版生产资源已部署至原访问地址。


---

### v0.9 — 2026-09-15｜缩小 Dating 卡片标题卡

**本次变更**

- 按最新反馈进一步缩小“关于见你这件事”标题卡。
- 收窄标题卡左右边界，减少上下内边距与圆角尺寸。
- 缩小标题、副标题和说明文字，全部强制单行显示。
- 标题卡与 DATE / TIME 信息卡之间保留更充足的固定安全间距。
- 其他已经正常的卡片内容和整体样式保持不变。

**验证结果**

- CSS 代码检查通过。
- `pnpm run build` 与 TypeScript 类型检查通过。
- 最新生产资源已部署至原访问地址。
